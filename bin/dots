#!/bin/zsh
#
# Master control for the dots framework shell commands. These can either
# be installed to 

PATH=$HOME/.dots/bin:$PATH
cd $HOME/.dots

export DOTS_COMMAND=$1

if [[ $COMMAND == ('help' || '') ]]; then
  rake -vT
else
  rake $1
fi
