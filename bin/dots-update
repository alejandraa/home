#!/bin/zsh
#
# A little shell script for maintaining your DOTS infrastructure.

cd ~/.dots
git pull --rebase origin master
source $HOME/.dots/vendor/antigen
ctags -R .
antigen update
source $HOME/zshenv
source $HOME/zshrc
